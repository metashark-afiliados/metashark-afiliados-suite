# .env.example
# ========================================================
#          VARIABLES DE ENTORNO CANÓNICAS
# ========================================================
# Copia este archivo a .env.local y rellena los valores.
# NO AÑADAS ESTE ARCHIVO AL CONTROL DE VERSIONES.

# --- Configuración de Supabase (Requerido) ---
# Se encuentra en tu Dashboard de Supabase > Project Settings > API
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_DB_URL=

# --- Configuración de la Aplicación (Requerido) ---
NEXT_PUBLIC_SITE_URL="http://localhost:3000"
NEXT_PUBLIC_ROOT_DOMAIN="localhost:3000"

# --- Configuración de Seguridad y Características ---
# Activa el modo de mantenimiento. Opciones: "true" | "false"
MAINTENANCE_MODE="false"
# Lista de IPs separadas por comas que pueden acceder al sitio en modo mantenimiento
MAINTENANCE_IP_WHITELIST="127.0.0.1, ::1"
# Lista de IPs separadas por comas a bloquear en el middleware de telemetría
IP_BLACKLIST=""

# --- Configuración de OAuth (Opcional) ---
# Lista de proveedores separados por comas. Ej: "google,github"
NEXT_PUBLIC_OAUTH_PROVIDERS="google"
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# --- Configuración de Sentry (Opcional, para producción) ---
# Se encuentra en tu Dashboard de Sentry > Project Settings
SENTRY_DSN=
SENTRY_ORG=
SENTRY_PROJECT=
SENTRY_AUTH_TOKEN=

# --- Herramientas de Desarrollo ---
# Activa el análisis del bundle de la aplicación. Opciones: "true" | "false"
ANALYZE="false"

# ===================================================================
#           MODO DESARROLLADOR (OPCIONAL, USO AVANZADO)
# ===================================================================
# ¡ADVERTENCIA! Activar este modo desconectará la aplicación de la
# base de datos remota y utilizará un servidor Supabase simulado en memoria.
#
# Úsalo SOLO para desarrollo de UI aislado y de alta velocidad.
#
# PROTOCOLO DE USO:
# 1. Para activarlo, descomenta la siguiente línea.
# 2. ANTES DE HACER COMMIT O MERGE, DEBES COMENTAR ESTA LÍNEA DE NUEVO
#    y verificar que la aplicación funciona correctamente contra la DB real.
# 3. Después de cualquier cambio en `schema.sql`, el archivo
#    `src/lib/supabase/dev-mock-server.ts` DEBE ser actualizado.
#
# DEV_MODE_ENABLED=true
# ===================================================================